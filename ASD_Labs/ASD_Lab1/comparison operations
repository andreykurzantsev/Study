#include <stdio.h>
int main() {
    float x;
    float y;
    printf("Enter your number: ");
    scanf_s("%f", &x);
    printf("Result: ");
    if (x <= -30) {
        y = 3 * x * x * x / 4 - 5;
        printf("%f\n", y);
    }
    else {
        if (x > -15) {
            if (x <= 3) {
                y = 4 * x * x + 2;
                printf("%f\n", y);
            }
            else {
                if (x > 20) {
                    y = 3 * x * x * x / 4 - 5;
                    printf("%f\n", y);
                }
                else { 
                    printf("The entered number is not included in the specified interval\n"); 
                }
        }
        
        
        }
        else {
            printf("The entered number is not included in the specified interval\n");
        }
    }
        
    return 0;
}
